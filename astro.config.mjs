import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://MaciekLichon.github.io',
    base: '/FM_Intro_section_with_dropdown_navigation',
});
